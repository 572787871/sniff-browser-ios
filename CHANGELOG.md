# 更新记录

## 0.6.1 - 2026-08-01

- HLS 分片下载现在按真实已写入字节计算并显示下载速度。
- 资源列表不再展示无法独立播放的 DASH/fMP4 分片，HLS 统一归入“视频”分类。
- 下载完成记录会从本地图片或视频文件异步生成真实预览图。

## 0.6.0 - 2026-08-01

- HLS 下载改为原生 Swift Playlist 解析、有限并发分片下载、磁盘断点和按清单顺序合并，不再把 `.m3u8` 或单个 `.m4s` 当成成品。
- 支持 Master Playlist 最高带宽 Variant、相对 URL、`EXT-X-MAP`、`EXT-X-BYTERANGE`、MPEG-TS 与 fMP4 输出。
- 普通图片优先使用适合短资源的直接传输；后台下载遇到可恢复的 URLSession 连接错误时自动进行一次前台传输回退。
- 下载完成后使用真实合并文件名与扩展名写入文件库；分片失败和合并失败显示明确中文原因。
- 直播、加密 HLS、DRM/FairPlay 仍明确拒绝，不进行破解或访问控制绕过。

本项目遵循语义化版本，并如实记录已经落地的能力。

## 0.5.0 - 2026-07-31

### 新增

- 将资源嗅探改为每个标签由用户主动开启；新建和恢复标签默认休眠，停止后保留结果但不再监听。
- 增加图片资源真实缩略图：请求去重、取消、ImageIO 下采样、5 MB 上限、内存/磁盘缓存及无痕仅内存策略。
- 增加 background URLSession 普通文件下载，支持真实进度、平滑速度、剩余时间、暂停 Resume Data、继续、取消、重试和后台事件接续。
- 增加 `AVAssetDownloadURLSession` HLS 离线下载，支持未受保护的有限时长 VOD，并拒绝直播与 DRM/FairPlay。
- 增加下载任务原子 JSON 持久化、并发调度、响应状态/HTML 错误页校验、本地通知和敏感请求上下文隔离。
- 文件库接入真实完成任务，支持缩略图、AVPlayer、Quick Look、分享、重命名和二次确认删除。
- 增加参考实现与许可证边界、开源项目说明文档。

### 安全与边界

- Cookie 只按目标 URL 从当前标签临时取得，不写入任务 JSON 或日志；不持久化 Authorization。
- 不引入 GPL 项目源码，不打包 Node.js、Electron、Android 组件、FFmpeg 或浏览器扩展。
- HLS 不转换为 MP4；Blob、直播、DRM/FairPlay 不创建假下载任务。
- 首次下载媒体显示合规说明，要求用户只保存有权获取的资源。

### 验证说明

- 当前 Linux 宿主没有 Xcode、Simulator 或 iPhone，无法执行 Apple 工具链构建与真机后台下载验证。
- 本地仅执行 Swift 语法树、YAML、plist、diff、资源脚本与敏感信息静态检查；Push 后由既有 GitHub Actions 负责真实编译和 XCTest，本轮不等待结果。

## 0.4.0 - 2026-07-31

### 新增

- 接入第一阶段真实资源嗅探，覆盖 DOM、动态节点、PerformanceResourceTiming、Fetch、XHR、媒体事件和导航响应。
- 增加完整资源模型、MIME/扩展名分类、规范 URL 去重和多来源元数据合并。
- 增加每标签独立的运行时资源仓库；导航或关闭标签时清理，无痕结果不持久化。
- 资源页面实时展示扫描状态、分类数量、文件名、格式、来源域名、大小、分辨率、时长和 MIME Type。
- 增加真实手动重新扫描、工具栏资源数量角标、复制链接、分享和资源详情。
- 增加分类器、去重器、消息解码和标签资源仓库测试。

### 安全与性能

- 注入脚本只通过唯一受限消息通道上报候选，不读取响应体、不修改 Fetch/XHR 参数、不执行额外 HEAD 请求。
- MutationObserver 与消息发送均批量节流，候选数量设上限，Swift 端在后台解码并按原始消息顺序合并。
- 消息拒绝 javascript/data/about、超长 URL 和错误字段；Blob 仅标记为不可直接下载。
- 日志只记录扫描阶段与数量，不记录 Cookie、Authorization 或完整敏感 URL。

### 限制

- 本版本只识别与展示资源，尚未开放实际下载。
- WKWebView 不公开所有底层请求；跨域元数据、Blob、加密 HLS 可能不完整或不可用。
- 不支持 DRM/FairPlay 解密，不绕过登录、付费或网站权限。

### 验证说明

- 当前宿主环境没有 Xcode、iOS Simulator 或 iPhone 真机，未执行本地 Apple 工具链编译和真机验证。
- 本地执行资源脚本 JavaScript 语法、YAML、差异格式、工程引用和敏感信息静态检查。
- Push 会触发现有 GitHub Actions；依照本轮要求不等待或轮询构建结果。

## 0.3.0 - 2026-07-31

### 新增

- 标签页管理改为竖屏两列两行首屏四卡片布局，更多标签垂直滚动；横屏根据宽度使用三列或四列。
- 普通与无痕标签组通过原生水平分页切换，并分别保留垂直滚动位置。
- 实现本地收藏模型、Application Support 原子 JSON 持久化、URL 规范化去重、搜索、打开、分享、复制和确认删除。
- 浏览器更多菜单会根据当前真实页面显示“添加收藏”或“取消收藏”，收藏变化同步更新收藏页和用户中心数量。
- 增加网页主题色服务：按 `theme-color`、`body`、`html` 背景顺序检测，解析 HEX/RGB/RGBA，并自动选择可读的顶部前景色。
- 增加统一下载设置路由和持久化下载偏好页面；下载空状态与设置首页进入同一详情页。

### 修复

- 用户中心头像、登录操作和四项统计在首次布局时同步创建，不再等待数据后展开；异步更新只修改数字。
- 删除用户中心重复的“浏览器设置”入口，并收紧剩余列表间距。
- 页面开始导航时恢复默认浏览器顶栏颜色，切换标签时恢复各标签已检测的主题色。
- 收藏失败时显示真实错误，不再产生成功反馈；新标签页、错误页和无效 URL 禁止收藏。

### 验证说明

- 当前宿主环境为 Linux，没有 Xcode、iOS Simulator 或 iPhone 真机；工程文件已在临时 Swift 容器中使用 XcodeGen 2.46.0 重新生成。
- 本地执行了 YAML 解析、Swift 语法树、主题脚本、工程引用、差异格式和敏感测试站点扫描；编译与 XCTest 未在本地执行。
- Push 会触发既有 GitHub Actions；依照本轮要求未等待或轮询构建结果。

## 0.2.0 - 2026-07-30

### 新增

- 实现真正的多标签浏览器：每个标签拥有独立 `WKWebView`、URL、标题、加载状态、前进后退状态和网页快照。
- 增加普通与无痕标签分组；无痕标签使用非持久网站数据存储，不写入普通会话。
- 增加普通标签基础会话恢复、最多 30 个标签的明确上限，以及最多 6 个常驻 `WKWebView` 的内存策略。
- 增加标签快照服务；内存压力或常驻数量超限时，按最近最少使用顺序休眠非活动标签。
- 将标签管理重做为自适应两列卡片，支持真实切换、关闭、新建、上下文操作及轻量底部工具栏。
- 将更多菜单重做为原生 Bottom Sheet，快捷操作与下载、文件、历史、用户、设置入口分组呈现。
- 增加可注入的用户中心本地数量模型；尚未接入仓库的数据诚实显示为 0，不使用示例数量。
- 增加多标签、浏览器展示状态和用户中心数量的回归测试。

### 改进

- 将地址栏压缩为 46 点、底部工具栏压缩为 54 点，扩大网页可视区域。
- 地址栏与工具栏改用动态超薄系统材质、极淡边缘与克制阴影，并适配 Reduce Transparency。
- 页面滚动采用稳定阈值的两段式紧凑状态，避免持续修改 `WKWebView` 布局引发跳位和抖动。
- 统一设置、用户中心、文件、下载、历史、收藏和资源页面的玻璃卡片、列表与可执行空状态。
- 清除浏览数据在二次确认后使用公开 WebKit API 移除 Cookie、网站存储与缓存，并重新载入当前页面。
- 补充深色模式、Dynamic Type、VoiceOver、Reduce Motion 和小屏布局约束。

### 验证说明

- 当前开发环境为 Linux，没有 Xcode、iOS Simulator 或 iPhone 真机。
- 本地只执行静态检查；工程生成、编译、自动测试和未签名 IPA 结果以本次提交对应的 GitHub Actions 为准。
- 不包含完整资源嗅探、后台下载、HLS 合并、Supabase 登录、媒体播放器或内容过滤规则。

## 0.1.0 - 2026-07-30

### 新增

- 从零创建 Swift + UIKit、iOS 17、iPhone-only 工程。
- 以 XcodeGen 管理工程配置。
- 建立完整设计系统和统一基础 UI。
- 实现 WKWebView 浏览器、新标签页、地址栏、工具栏和基础导航。
- 建立资源、标签、收藏、历史、下载、文件、账户、设置页面骨架。
- 定义资源识别、下载和认证协议。
- 添加基础服务与 XCTest。
- 添加 GitHub Actions 未签名 IPA 构建。
- 添加原创 1024×1024 App Icon 与动态 Accent Color。
- 在 GitHub 托管 macOS Runner 完成 XcodeGen、模拟器编译、单元测试、iphoneos Release 和未签名 IPA 打包的全链路验证。
- 提交由 `project.yml` 生成的 `SniffBrowser.xcodeproj`，同时保持 XcodeGen 配置为工程唯一真实来源。
- 修正 XcodeGen 资源阶段配置，将 App Icon、Accent Color 与本地化资源纳入 App 资源构建阶段。
- 增加 CI 产物断言，检查 `Assets.car`、主 App Icon 元数据与 iPhone-only 配置；是否通过以对应最终提交的成功工作流为准。
- 完善 Reduce Motion、Reduce Transparency、Dynamic Type、VoiceOver 和小屏滚动边界适配。
- 未接入真实服务的资源预览、下载和无痕操作不再伪装为可用；下拉刷新会持续到网页加载结束。

### 说明

- 不包含任何旧项目代码或迁移内容。
- 当前无 macOS Simulator 和 iPhone 真机验证。
- 不伪造资源、下载、登录和测试结果。
- 资源识别、实际下载、文件操作和持久化账户留待后续阶段实现。
