# SniffBrowser 产品能力矩阵

本文件是产品范围与交付状态的单一核对表。代码、界面文案和测试不得把“页面存在”误写成“能力完整”；真机未验证的 WebKit、后台任务和手势体验也不得标记为已验证。

状态说明：

- **已实现**：主闭环已有真实数据与错误反馈，并有相应代码或自动测试。
- **已实现／待真机**：代码与 CI 可验证，但关键体验仍依赖 iPhone、网络或具体网站。
- **部分实现**：有可用子集，限制必须在产品中如实呈现。
- **未配置**：只有接口或界面，不应承诺可用服务。

## 核心能力

| 能力 | 当前状态 | 状态与数据归属 | 自动验证 | 仍需验证／明确限制 |
| --- | --- | --- | --- | --- |
| WKWebView 浏览、搜索、前进后退、刷新 | 已实现／待真机 | 每个标签独立 `WKWebView`；浏览状态由标签模型持有 | 浏览器状态与导航相关 XCTest、GitHub Actions 编译 | 具体站点兼容、键盘、旋转和滚动手感需真机 |
| 新标签页与主页背景 | 已实现／待真机 | 外观偏好与非敏感背景选择持久化；自定义图片只在沙盒 | 设计系统与配置测试、CI 编译 | 小屏、横屏、深色及自定义照片可读性需真机 |
| 普通标签管理 | 已实现／待真机 | 普通标签会话版本化保存；快照用于预览，不替代真实网页 | 标签模型、会话、布局和转场相关测试 | WebKit 进程恢复、内存压力和连续转场需真机 |
| 无痕标签 | 已实现／待真机 | 非持久 `WKWebsiteDataStore`；不进入普通会话；关闭即释放资源状态 | 标签隔离测试 | 系统 WebKit 缓存边界与内存压力需真机 |
| 收藏夹 | 已实现 | 原子 JSON、规范 URL 去重；单条删除后 5 秒内可撤销 | Repository、Service、ViewModel 测试 | favicon 网络加载受站点策略影响 |
| 历史记录 | 已实现 | 原子 JSON；短时间同 URL 合并；单条删除可撤销，全部清空需确认 | Repository、Service、ViewModel 测试 | 大数据量性能与跨版本迁移需继续覆盖 |
| 网站权限 | 已实现／待真机 | 摄像头、麦克风、位置的默认策略和按站点决定本地保存 | 持久化与策略测试、CI 编译 | 系统授权弹窗和不同站点调用方式需真机 |
| 内容拦截 | 已实现／待真机 | 本地规则、导入规则、白名单和统计持久化 | 规则解析、存储与 CI 编译 | WebKit 规则命中、规则源更新和大规则集性能需真机 |

## 资源与文件能力

| 能力 | 当前状态 | 状态与数据归属 | 自动验证 | 仍需验证／明确限制 |
| --- | --- | --- | --- | --- |
| 资源嗅探启动 | 已实现／待真机 | 每个标签默认未开启；只能由用户主动开始；切换标签或关闭面板停止监听 | 主动启停、标签隔离、消息解码测试 | WKWebView 不公开全部底层请求，结果不保证穷尽 |
| 资源列表筛选与排序 | 已实现 | 筛选、排序跟随当前标签会话；关闭标签后清除 | `TabResourceStoreTests` | 搜索词不持久化；这是有意的临时输入规则 |
| 图片缩略图 | 部分实现／待真机 | 普通标签可使用受限内存与磁盘缓存；无痕只用内存 | 图片解码、缓存、请求相关测试 | 防盗链、跨域、Blob、超大图和特殊编码可能失败；失败必须显示明确状态 |
| 视频封面与在线播放 | 部分实现／待真机 | 视频封面按资源缓存；播放使用 AVPlayer，HLS 可经本机代理携带当前请求上下文 | 元数据、代理和缓存相关测试 | 签名 URL、站点鉴权、编码兼容和过期链接可能导致失败 |
| 普通文件下载 | 已实现／待真机 | background URLSession；任务与恢复数据持久化；筛选跨启动保留 | 下载状态、持久化、策略测试 | 后台杀进程、蜂窝切换和服务器 Resume Data 兼容需真机 |
| HLS VOD 下载 | 部分实现／待真机 | 原生清单解析、分片检查点、本地播放包；支持公开 VOD 与 identity AES-128 | Parser、下载状态、加密与本机媒体服务测试 | 不支持直播、SAMPLE-AES、非 identity KeyFormat、DRM/FairPlay；不绕过权限 |
| 媒体后处理 | 已实现／待真机 | 正式 CI 构建内置固定版本 libav XCFramework；本地无依赖配置使用明确 Stub，不伪造处理成功 | Media Pipeline 与正式 FFmpeg 集成测试、IPA 链接硬校验 | 编码器/容器兼容和长媒体内存、温度、耗时需真机 |
| 下载中预览 | 已实现／待真机 | 使用当前有效分片／临时文件和持久封面，不等待任务完成才显示反馈 | 下载模型与缩略图回归测试 | 可拖动范围取决于已缓存区间、容器和服务器能力 |
| 文件管理与播放 | 已实现／待真机 | 只展示真实完成文件；分类与排序跨启动保留 | 文件模型、下载联动与 CI 编译 | 大文件、Quick Look、AVPlayer、分享及沙盒容量需真机 |

## 账户、交付与边界

| 能力 | 当前状态 | 说明 |
| --- | --- | --- |
| 账户与云同步 | 未配置 | 已有认证协议和游客界面，但没有可用后端；不得显示虚假登录或同步成功。 |
| 主题与无障碍 | 已实现／待真机 | 五种强调色、语义深浅色、Dynamic Type、VoiceOver、Reduce Motion/Transparency 基础已接入；仍需真机审计。 |
| 自动化构建 | 已实现 | GitHub Actions 生成工程、Simulator 编译与 XCTest、iphoneos Release 编译并产出未签名 IPA。 |
| 签名与发布 | 部分实现 | CI 产物未签名，不能视为 App Store 或可直接安装包；签名、商店资料与审核尚未完成。 |

## 统一产品规则

### 展示状态保留

- 资源嗅探的分类与排序属于**标签会话**：关闭再打开同一标签的资源面板会恢复，关闭标签后清除，普通与无痕互不串用。
- 下载页筛选、文件页分类与排序、内容拦截统计范围属于**应用展示偏好**：跨页面和重启保留，不包含网址、Cookie、搜索词或其他敏感数据。
- 搜索词、临时选择和弹窗状态只在当前页面存在，不持久化。
- 标签总览普通／无痕滚动位置分别在当前应用会话中保留，并在数据变少时限制到有效范围。

### 删除与错误反馈

- 单条收藏和单条历史记录使用“立即从列表移除 + 5 秒撤销”，避免为低风险操作堆叠确认框。
- 批量清空历史、删除本地文件及其他不可轻易恢复的操作必须确认。
- 失败不能显示成功反馈；空状态不能填充示例数量或历史结果。
- 尚未实现的入口不出现在菜单中。只有具备动作、结果和错误反馈的能力才能对用户可见。

### 不可回退约束

- 资源嗅探必须由用户主动开启，且结果严格按标签隔离。
- 标签转场只动画临时快照，不能复用参与过动画的 `UIView`，也不能缩放真实 `WKWebView`。
- 普通预览缓存和无痕预览缓存必须分开；无痕资源不得写磁盘。
- 不绕过 TLS、登录、付费、DRM 或网站访问控制；不把不支持的媒体伪装成下载成功。

## 验证口径

Linux 工作区只能完成差异、配置和源码静态检查。GitHub Actions 成功表示工程生成、Apple 工具链编译和自动测试通过；只有 iPhone 真机测试才能确认 WebKit 兼容、动画手感、后台下载、系统权限、内存和媒体播放体验。
